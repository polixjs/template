# template
Polix initializes the template

## Install
```bash
$ npm i polix-cli -g
$ pol init example && cd example
```

## Start
```bash
$ make dev
# or
$ ./bin/build.sh && cd dist && node index.js
```